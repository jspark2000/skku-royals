<!DOCTYPE html>
<html>
  <head>
    <%- include('../../../common/admin-html-head') %>
  </head>
  <body>
    <%- include('../../../components/admin-page-header', { userInfo } ) %> <%- include('../../../components/admin-page-sidebar', { select: 'people', select_child: 'people/register' } ) %>

    <main id="main" class="main">
      <div class="pagetitle">
        <h1>부원 등록</h1>
        <nav>
          <ol class="breadcrumb">
            <li class="breadcrumb-item"><a href="/admin">관리자 페이지</a></li>
            <li class="breadcrumb-item active">부원 등록</li>
          </ol>
        </nav>
      </div>
      <!-- End Page Title -->

      <section class="section">
        <div class="row align-items-top">
          <div class="col-lg-4">
            <div class="card overflow-auto">
              <div class="card">
                <div id="target" class="card-body">
                  <p class="pt-4">밴드 계정을 선택하면 이곳에 신규 부원 등록 폼이 생성됩니다.</p>
                </div>
              </div>
            </div>
          </div>
          <div class="col-lg-8">
            <div class="card recent-sales overflow-auto">
              <div class="card-body">
                <h5 class="card-title">밴드 계정 목록 테이블</h5>
                <table class="table table-borderless datatable">
                  <thead>
                    <tr>
                      <th scope="col">#</th>
                      <th scope="col">밴드 닉네임</th>
                      <th scope="col">식별키</th>
                      <th scope="col">선택</th>
                    </tr>
                  </thead>
                  <tbody>
                    <% bandUserList.forEach((bandUser, index) => { %>
                    <tr>
                      <th scope="row"><a href="#"><%=index + 1%></a></th>
                      <td><%=bandUser.userNickname%></td>
                      <td><%=bandUser.userKey%></td>
                      <td><button class="btn btn-outline-danger" onclick="registerPeople('<%=[bandUser.userKey, bandUser.userNickname].join()%>')">선택</button></td>
                    </tr>
                    <% }) %>
                  </tbody>
                </table>
              </div>
            </div>
          </div>
        </div>
      </section>
    </main>
    <!-- End #main -->

    <%- include('../../../common/admin-footer') %>

    <a href="#" class="back-to-top d-flex align-items-center justify-content-center"><i class="bi bi-arrow-up-short"></i></a>

    <%- include('../../../common/admin-js') %>
    <script src="/public/admin/js/people.js"></script>
  </body>
</html>
